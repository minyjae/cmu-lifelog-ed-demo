# 🌐 Backend API URL (ใช้ใน client-side)
NEXT_PUBLIC_API_URL=http://localhost:8080/queue-doc-api/api

# 🌐 Backend API URL (ใช้ใน server-side / API routes)
INTERNAL_API_URL=http://localhost:8080/queue-doc-api/api

# 📁 Base Path (เว้นว่างไว้ถ้าไม่ได้ deploy ที่ sub-path)
NEXT_PUBLIC_BASE_PATH=
